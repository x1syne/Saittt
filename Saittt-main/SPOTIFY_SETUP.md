# 🎵 Настройка Spotify OAuth для SoundMate

## Проблема
Не работает авторизация через Spotify из-за неправильной конфигурации Redirect URI.

## Решение

### 1. Настройка Spotify Developer Dashboard

1. Перейдите на https://developer.spotify.com/dashboard
2. Войдите в свой аккаунт Spotify
3. Откройте ваше приложение (или создайте новое)
4. Нажмите **"Edit Settings"**
5. В разделе **"Redirect URIs"** добавьте:
   ```
   https://saittt.vercel.app/auth/spotify/callback
   ```
6. Нажмите **"Add"**, затем **"Save"** внизу страницы

### 2. Настройка переменных окружения в Vercel

1. Перейдите на https://vercel.com/dashboard
2. Откройте ваш проект `saittt`
3. Перейдите в **Settings** → **Environment Variables**
4. Добавьте следующие переменные:

   ```
   SPOTIFY_CLIENT_ID = ваш_client_id_из_spotify_dashboard
   SPOTIFY_CLIENT_SECRET = ваш_client_secret_из_spotify_dashboard
   REDIRECT_URI = https://saittt.vercel.app/auth/spotify/callback
   ```

5. Нажмите **"Save"**
6. Перейдите в **Deployments** и сделайте **"Redeploy"** последнего деплоя

### 3. Проверка работы

После настройки проверьте:

1. Откройте https://saittt.vercel.app/ - должна показаться страница с информацией о конфигурации
2. Убедитесь, что Client ID и Client Secret показывают ✅
3. Нажмите кнопку "Войти через Spotify"
4. Должно открыться окно авторизации Spotify
5. После авторизации вы будете перенаправлены на dashboard

### 4. Возможные проблемы

#### Ошибка "INVALID_CLIENT"
- Проверьте, что Client ID и Client Secret правильно скопированы
- Убедитесь, что нет лишних пробелов в начале/конце

#### Ошибка "Redirect URI mismatch"
- Убедитесь, что в Spotify Dashboard добавлен точный URL: `https://saittt.vercel.app/auth/spotify/callback`
- URL должен совпадать полностью, включая протокол (https://)

#### Токен не сохраняется
- Проверьте консоль браузера (F12) на наличие ошибок
- Убедитесь, что localStorage не заблокирован в настройках браузера

### 5. Структура проекта

```
Saittt-main/
├── api/
│   └── index.js          # Backend API (деплоится на Vercel)
├── index.html            # Главная страница
├── callback.html         # Обработка callback от Spotify
├── dashboard.html        # Личный кабинет пользователя
└── vercel.json          # Конфигурация Vercel
```

### 6. Как это работает

1. Пользователь нажимает "Войти через Spotify" на index.html
2. Перенаправление на `https://saittt.vercel.app/auth/spotify`
3. Backend перенаправляет на Spotify для авторизации
4. Spotify перенаправляет обратно на `/auth/spotify/callback` с кодом
5. Backend обменивает код на access token
6. Backend перенаправляет на `callback.html` с токеном
7. callback.html сохраняет токен в localStorage
8. Перенаправление на dashboard.html
9. dashboard.html загружает данные пользователя через API

### 7. Тестирование

Откройте консоль браузера (F12) и следите за логами:
- При клике на кнопку должно быть: "Начинаем авторизацию через Spotify..."
- На странице callback: "Callback page loaded", "Data saved to localStorage"
- На dashboard: "Loading user data..."

## Готово! 🎉

После выполнения всех шагов авторизация через Spotify должна работать корректно.
